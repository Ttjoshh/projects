export interface Note {
    id: string;
    content: string;
    author: string;
    createdAt: string;
    color: string;
  }
  
  export interface User {
    id: string;
    name: string;
  }